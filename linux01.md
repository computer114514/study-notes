##### >给你一台新电脑和项目。你如何部署进linux？

学这个的目的，是为了让你重装的时候不用翻教程，很掉价，同时彻底理解linux的安装流程。同时应对面试拷问。

1，下载vmware

2，弄好虚拟机配置和centos镜像。软件选择、安装位置

#### 3，Linux目录结构

/ -----------home目录

/bin -------bin代表软件运行所需要的二进制文件。脚本。exe,dll,so.

/dev ------设备文件。

/etc -------环境变量设置

/lib --------依赖

/media --------外设

/proc -------内存映射

/run,srv -------临时文件(运行)

/sys

/tmp -------临时文件清理

/usr --------用户环境

/var ---------日志。 

#### 4.linux网路配置

4.1主机模式、桥接模式、NAT模式

采用nat模式，网络配置之192.168.6.0

随后设置ipv4的192.168.6.100，网关是192.168.6.2,子网掩码是255.255.255.0

###### 5，保存快照

###### 6，克隆集群

快速搭建很多台服务器

###### 7，xshell、finalshell

8，帮助命令man

###### 9，虚拟机开关机命令

#### 10，服务管理类命令

##### 11，目录命令

不要删除mv改.bak文件。

##### 12，查看命令

1，head -n 文件名

查看文件里边有什么。

###### 2，tail输出文件尾部内容。

tail -5 文件名。

查看日志，因为错误在最后面，所以需要查看log，tail -n -5，查看的就是tail了。

###### 13，输出echo，覆盖>

###### 14,链接，相当于快捷方式。

###### 15，history命令，可以写入一个文件

###### 16，vim文件编译器修改配置文件。插入命令模式

17，主机名称

>还好我保存了快照，不然就完了，这个快照不就是应对我把虚拟机搞崩的情况吗？！
>这才是应用口牙。

###### 18，用户管理

useradd tangseng，passwd设置密码。

##### 19，sudo

让普通用户拥有root权限。

###### 20，组管理类任务。

把用户归于一个个组罢了。

21，文件权限管理命令

drwxr-xr-x

##### 22,加权限

chmod o+w software/

755!755!

##### 23,搜索查找类命令。

find xiyou/ -name "*.txt"

名称、拥有者、大小。

##### 24，grep过滤 

find xiyou/ -name *.conf

ll |grep txt 

##### 25,解压缩命令

tar命令。tar -zcvf name source x n 

tar -zxvf name

find命令找日志。

##### 26,磁盘分区类命令  

挂载，插入的光盘dvd磁盘分区什么的需要挂载。 

##### 27，任务管理器。

kill 

ps -ef

##### 28,系统定时任务命令

crond服务之定时任务。时间表示的逻辑。

##### 29，java开发扩展。

29.1rpm下载好，是离线的。

29.2yum联网下 