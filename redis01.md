# Redis

学习技术之后就要应用到项目上，千万记住！

##### 1，SQL:关系表，结构化表

数据有约束

不好修改

ACID

一致性

磁盘

>安全性高，结构固定

##### 2，NoSql 

字段约束少

无关联的 

内存

>数据结构不固定，一致性安全性不高，性能要求

##### 3，redis

Remote Dictionary Server

远程字典服务

##### 4，安装

基于linux服务器部署。
服务器大多使用linux部署
1，24时运转，提供服务，且长期不需要重启
2，性能比windows更好
3，命令行功能强大，兼容性好

4.安全性好

##### 5，配置

1，配置conf文件，设置什么端口，密码，日志输入的地方。

2，设置开机自启动

### 6，基本类型和使用

1，可以在网站查大多数。
2，通用命令:expire,exists,ttl,keys,del

##### 3,String类型

>set
>
>get
>
>mset
>
>mget
>
>incr自增一个数组
>
>incrby
>
>INCRBYFLOAT
>
>setnx 如果不存在就set
>
>setex 直接指定有效期

##### 4，Hash类型

> 大多命令加上h就行。
>
>HGETALL
>HSETNX

##### 5，List类型

>LPUSH 
>
>LPOP
>
>RPUSH
>
>RPOP
>
>LRANGE

##### 6，set类型

>SREM移除这个元素
>
>SISMEMBER有没有这个元素
>
>SCARD所有元素个数
>
>SINTER交集

##### 7，sortedSET类型

1，添加了score机制，相当于一个分数

##### 8，jedis

springdatatemplate,在idea中使用redis,这jo是java客户端

##### 9，序列化错误

需要配置如何如何序列化。遇到了版本不一致的问题，自己捣鼓出了解决方案。

以后遇到了序列化问题要想到需要配置序列化方法。

##### 10，对象能否序列化?!

可以的，stringredistemplate自己手动序列化反序列化呗。

##### 11,那么到底哪里可以使用redis呢？

社交平台、电商

### 12，短信登录

登录:验证码放redis，登录凭证放redis去找。

### 13，商品缓存

>json转对象用fastjson、jackson、hutool'
>
>普通字符串转java对象用hutool，convert
>hutool:
>对象转json:JSONutil.toJsonStr.
>
>json转对象
>
>:JSONutil.toBean();

##### 1,基本缓存

##### 2，缓存更新策略

>为了防止缓存一致性问题

最佳:自带的内存淘汰

高一执行;主动更新。先写数据库再删缓存

3，删除缓存策略比更新缓存策略更好。

##### 4，缓存穿透

防止多次请求，在redis中
1，null值缓存：查到null就在redis缓存空(有浪费内存的风险)

2，布隆过滤：加一层

3，限流啊什么的，主动防御攻击

##### 5，缓存雪崩-->redis宕机或者key集体失效导致的请求涌入服务器造成破坏

1，TTL加随机值
2，降低限流。
3，多级缓存。一层没了就没了还有很多层

##### 6，缓存击穿。高并发访问的key失效，缓存重建复杂的key

1，互斥锁。一个线程有锁，其他线程没锁不能访问，**等待**着有锁。

2，逻辑过期。过期了，就开薪线程更新，但是**返回旧数据**。

##### 7,工具类封装

##### 8,缓存更新策略

### 9,优惠券秒杀

#### 1，全局id处理器，保证分布式系统下id不重复

1，redis自增策略:符号位+时间戳+序列号()

2,UUID

3,snowFlake算法-->时间高依赖

4，数据库自增

redis原子自增解决并发问题。

#### 2，典中典之超卖问题

悲乐观锁！！！！！！！！！
可以写在简历上的。

##### 悲观锁

直接串行

安全但是性能差。

##### 乐观锁--->放超卖

扣减余额的时候去**判断有没有其他线程对数据做了修改**。

不安全但是性能还行

###### 乐1，版本号法。

数据加上version版本号

###### 乐2，CAS法

或者加上**真实数据**，比如扣之前看看库存满不满足要求

#### 3，典中典防黄牛之一人一单

```
synchronized之加悲观锁
```

#### 4，分布式锁-->一人一单

>简历绝对不能写的分布式锁，现在我要学了

嗯，服务中的，统一的，都可以获取的，性能要好的。那不就是redis嘛!

###### 4.1reids实现分布式锁

~~~redis
set lock thread1 NX EX 10
~~~

手动释放，不阻塞，尝试一次

###### 4.2极端情况锁的误删情况。

 业务逻辑过长导致锁删除时间到了和业务结束自动删除冲突。**删除了下一个线程的锁**。出现超卖等线程安全问题

4.2.1lua脚本解决原子性问题，保证判断和执行是一起的。

~~~lua
-- 锁的key1
local key=KEYS[1]
-- 当前线程表示
local threadId=ARGV[1]
-- 锁中线程表示
local id=redis.call('get',key)
-- 比较
if(id==threadId) then
  return redis.call("del",key)
    end
return 0
~~~

# 找不到大二实习你就考研去吧！

###### 4.3一些问题。

1，锁不可重入(调用线程导致死锁)

2，不可重试

3，超时释放

4，主从一致

###### 4.4redisson封装。

>1,引入依赖
>
>2，配置bean
>
>3，使用

redisson原理小知识。

---解释redisson实现重入。

重入场景是指一个线程方法调用同一线程另一个获取锁的方法。

当重入时，判断redis中有没有这个锁，如果有就加一，重置过期时间。没有这个锁就添加上这个锁。

释放锁时，判断锁是不是自己线程的，如果是那就减一同时判断是不是0，是0就真正的del释放锁，不是0就简单减一。不是自己线程的啥也不干

---解释redisson重试机制。

redisson:一个请求发过来，发现锁已经被获取了，而且不是当前线程的，那么就会通过消息队列等待锁儿释放，释放之后重试获取锁，获取失败就再重试，知道成功，返回null的剩余时间。如果这其中传入的等待时间到了，那么就退出，返回剩余时间

---redisson看门狗机制。

看门狗watchdog等剩余时间减少之后继续重置有效期，直到锁被释放发出信号，看门狗才会停止重置。

---Redisson的multiLock

为了防止服务失败，运用这个连锁机制，必须从所有reids节点获取到重入锁，才算获取锁成功。

这样一台坏了就获取失败了呗，至少安全问题没有多少了。

### 5,Redis优化秒杀

>运用redis直接返回id，然后**异步**给tomcat保存订单信息。
>
>redis是前台，tomcat是厨房，两者不分离。

1，redis判断库存信息和一人一单的判断。

2，redis存储库存100.

3，redis存储购买过的用户信息。需要唯一性。

------->set，唯一。

4，先判断库存。然后判断用户是否真正购买过。userId存储set

5，redis使用lua脚本。根据结过为0-->保存到**阻塞队列**

编写lua脚本。执行。记得要传字符串，然后直接返回前端，剩下的交给阻塞队列了

阻塞队列:没有获取到数据时，阻塞线程，直到获取到数据。
6,异步下单：开启一个新线程，这个线程处理你的保存到数据库业务

>ju局限:1，数据安全问题。2，内存限制问题。 

### 6，Redis消息队列。

解决断电内存消失问题。 

阻塞队列要占用jvm内存，安全性没保证。消息丢失。

1，list实现消息队列，BRPOP，来模拟消息队列

------->但是消息丢失无法避免。只支持单消费者(拿走了就是拿走了 )

2，pubsub多生产多消费

------->不数据持久化，消息丢失，超出消息堆积上线就消失。

#### 3，stream消息队列

> 这个就厉害了

是一种数据类型。有很多操作。

>消息持久，多个消费者读取，阻塞，但是有消息漏读的问题。

1，消息分流，分给组内不同消费者，加快消费者处理

2，消息标示:标记最后一个被处理的消息，避免丢失。

3，消息确认，必须要确认被处理了，才移除。 

##### 4，消费者组



#### 5，发博科和查博客的业务，

点赞业务:就是redis中一个key对应的用户，点赞用户加，然后数据库setlike++，然后查询的时候看看用户里有没有点赞，有就islike返回

排行榜:

需要redis存储userid，而且要有序，这就需要sortedSet了

因为sortedSetscore有序，唯一(根据score分数判断有无)，而且查找快

>ZADD,ZSCORE

小bug:需要自己orderBy filed添加id才可以正确排序。

#### 6，好友的关注

复习经典mysql操作。实现查询用户是否关注和关注用户业务。
复习crud;

##### 6.2共同关注。

set集合实现交集。SADD s1 m1 m2

SINTER s1,s2;

>嗯，关注的用户存入redis集合中，然后每个用户对应一个set，然后用UINTER交集。

##### 6.3feed流拉模式(用视频平台创作者和粉丝来举例)

1，内存节省 but 1，读取延迟高，因为要一个个拉过来

粉丝拉创作者。

##### 6.4feed流推模式。写扩散。

1，内存占用太大了，每个都要读一遍

创作者推给每个粉丝。

##### 6.5feed流拉推结合。

具备拉推两种模式优点。

好粉丝推，坏粉丝拉。

##### 6.6feed流滚动分页。----->不同于角标分页

发了信息，用户可以接受到。因为redis保存了feed列表，里面是用户的feed。

为了防止重复查询，使用sortedsort实现滚动分页。通过score记录每次最大的时间戳下次**从这个时间戳开始查询**。

然后用户查询推荐的时候，需要max和offset来获得数据。数据需要处理，加入isliked和queryUSer完善数据，最后手动order by field指定顺序。

小问题:score记录的可能有很多值，那么怎么重新找到呢。运用offset偏移，遍历raw结果的时候，os++，然后最小值每次都更。新，这样就能找到偏移量和max了。随后前端返回。

>小知识:queryWrapper构造条件可以自由构造条件，而query()不可以，只能固定死构造条件。

重原理，新语法。

##### 7地理位置(redis的geo数据类型) 

主要就是通过redis的geo获取查询到的数据，然后分好页，转化为shops。加入distance，

##### 8，bitMap实现签到。总的来说就是用bit对应01对应状态，节省空间的目的

redis的key里边是0000001110000000对应签到顺序。

8.1连续签到
小技巧：从最后一次签到统计，到第一次未签到的个数。

~~~js
BITFIELD key get u10 0.
获得到今天为止的所有bit位。

和1做与运算，得到最后一个bit位。右移。和1做与运算，得到最后一个bit位。右移。和1做与运算，得到最后一个bit位。右移。这就是遍历过程 
~~~

##### 9，UV PV统计。运用hyberLogLog数据结构

UV:一个用户访问多次记录一次。

PV:一个用户访问几次记录几次。

运用hberLoglog算法:概率论算法。有一点误差，但是即使数据量很高，内存占用特别低。

redis语法:

~~~js
PFADD hl1 e1 e2 e3
PFCOUNT hl1
~~~

##### 而且！不会重复

所以尤其适合UV;适合数据量大。

======================

保存一下，后面的内容后面再看。









