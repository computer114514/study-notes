##### 1，线程和任务。

>线程要执行任务
>
>多线程执行多个任务。

T.H.R.E.A.D。

线程是通过thread创建的。

任务通过runnable接口来定义。 

thread<------>runnable

张三<--------->前后端任务。

1，继承thread类

2，实现runnable接口✅️

3，实现callable接口

~~~js
class thread implements runnable

>就是类能够执行任务了。
~~~

thread构造器，可以带上任务。

~~~js
无参构造不需要指定任务，有参需要指定。
~~~

thread启动流程:

1，创建对象，指定任务

2，启动线程，start()等待获取cpu资源。

~~~js
Thread thread=new Thread(runnable);
thread.start();
//等待资源，执行target任务的run方法。
~~~

>补充:thread中定义了一个成员变量target.
>//1,创建thread对象时传入任务。
>//2,任务赋值给target成员变量。
>//3,线程执行，操作class thread中的成员变量target。

##### 2，继承和实现接口

###### 2.1继承

~~~js
//继承thread,便乘新thread。重写。
extends thread。
~~~

myThread类继承thread实现 run()

new MyThread().start();

>实现自己的run方法。

> 但是太太太耦合了

###### 2.2实现Runable接口。

2.2.1实现runnable,定义一个类runnable1，覆盖run()。

2.2.2创建类，把runnable1穿进去。

线程独立执行

>学到的操作系统知识用到了

###### 2.3优化

2.3.1匿名内部类来减少runnable的实现类。

~~~js
new Runnable(){
    @override
    public void run(){
        sout."niubi"
    }
}
//new的不是接口，是后面的匿名类
~~~

2.3.2用lambda表达式。

>只传方法的实现

~~~js
()->{
    sout."niubi"
}
~~~

##### 3，sleep()方法

1，创建t1对象并启动。

2，t1休眠1s

3，t2启动

###### 3.1，sleep的真面目。

>sleep方法休眠不在于**谁调用sleep**,而在于sleep写在哪

和调用者无关。

