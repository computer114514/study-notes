# OCM后台管理系统

>4月1日---5月1日

## 4/1

###### 后：

1,使用代码生成器codeGenerator，生成三层架构的代码。从mp官网抄。
2，复习sql命令建立表。tinyint用作boolean，bigint转换为long
3,运用openapi，网站访问接口文档。理解如何加了依赖便能访问网站的接口文档，访问的是依赖jar里边的静态文件
4,复习yml配置，数据库的链接，端口的配置。
5，复习result类编写，测试

6，构造条件，Admin：：getusername代表方法引用。解析为字段。

7，token:由类型+内容+签名组成，不是随机字符串！你不也不要被jwt吓到了。先写一个jwt(jsonWebTokens)工具类吧。

~~~js
复习jjwt：
1，从你的机密文件里获得token秘钥和过期时间
（采用配置类，来获取配置文件的属性，注解指定配置文件前缀，配置类里面string可以不加引号吗？）
2，先把string密钥key化为key。
Keys.hmacShaKeyFor(密钥字节数组)
3，采用jjwt的builder类已date,过期date,签名，和明面数据为组合，compact打包形成真正的string字符串
(注意，新方法是cliams再设置属性再设置签名再整合)
~~~

数据库最好int、tinyInt用INterger，而日期最好用LocalDateTime。


###### 前：

1，Promise好像是个处理异步操作的对象，对象.reject代表在主动终端程序

2,使用拦截器使得失败统一返回。

# 4/2

##### 后:

1，经典生成token，直接使用redis吧，免得后面又要重构。(最好存入id作为载荷，而不是用户名，不然被改了就完了)

2，redis怎么集合springboot？复习使用。（springredistemplate适合简化存储，redistemplate适合复杂存储，springredsitemplate不需要手动序列化所以用这个，）

~~~Js
redis复习
1，引入依赖springboot-starter-redis
2，配置spring reids prot host database
3，使用。
~~~

2.5validToken方法实现。

~~~js
jwts的解析构建器，设置签名，构建，解析token，没错就放行。(弃用)
jwts的parser()设置签名scretkey，然后再build(),解析
~~~

我发现，除了问ai，还有一种有效率的方法，就是阅读源码，现在知道了？

2.7parseToken方法实现

~~~js
上一步解析后获得载荷，或者id
~~~

3，拦截器拦截不合法的请求。逻辑是判断redis中有没有这个token和检查有效性。

##### 前：

引入useCookie，有点困难一个路径错了全都错了，页面打不开了。前端最该死的地方。

安装新包后如果不强制清除缓存，它可能依然认为 `change-case` 不存在

学会了uninstall命令。

~~~js
我真受不了了，浪费两小时搞这个vueuse。
1:反复报错是因为我装错路径了，我装到上一次了，我装到父级目录了，我装到错误的地方了，根本原因还是因为我没在正确的地方安装这个依赖。一定要牢记不要装错地方。
2.然后是intergrations里边还有无数的依赖要装，怎么回事？不应该一次性装好吗？
~~~

3，前端就是要提示错误才对啊。用好看的提示方式啊

4，请求拦截器修改了config，要返回回去，因为把config交给你，不返回怎么行呢，reading xxx，代表正在读取xxx
5，import "@/permission/perimission";意思是跑一下。import{}from是先跑一下然后拿东西;

6,pinia插件和usePinia()问题，最终解决方案是把usePinia()弄到onMOunted里面就好了，可能是因为调用的时候，有了插件的拖延，pinia还没有准备好，所以需要onMOunted挂载完之后导入

7，状态管理(pinia)和获取异步数据(await)就是这样的，需要在onMOunted，这是最佳实践。

# 4/3

**前:**1，运用了vuerouter4重构路由守卫。

2，加入全局加载条，nProgress

3，script少加了个setup，补上了，不然组件是无法正确现实的

4，头部样式开发，tailcss遇到很大问题。

##### 后：

1，linux，ps -ef显示所有进程
2，结果是redis配置文件错了，真该死啊。spring:data:redis这样才行，害我折腾半天

4/4

前:
1，不会阅读英文官网的坏处

vue看成react了。该死的，或许我该多背背单词了。
最终的结论是，**style.css我没有在main.js引入**，这是我在创建的时候埋的坑。看了我要留意我的每一个行为了。

2，成功引入tailwindcss4用户构建样式。装插件，不然没有提示，不好受
3，删依赖的新方法:跑到packagejson删除依赖之后，执行dev install安装dependcies。
4，有边距怎么办

5，教程是windicss，你用的tailwindcss，不一样，要记住

后:

1,退登、修改密码、refresh拦截器

2，完成userholder

3，学习多线程基础。

4/5

##### 前:

##### 后:

>昨天完成了UserHolder类，可以从线程局部变量取出用户对象

##### 1，编写refreshToken拦截器

- 我想,拦截器调一次sql,取出admin,存入线程局部变量,以后就jo不用反复调sql了吧
- 更新token过期时间,用stringboot的stringRedisTemplate

##### 2，编写token拦截器。

- 返回错误的时候，设置status还要设置返回体。告诉前端这是个json数据。
- 对了，add路径的时候，add("/*")是一级目录，add("/**")是多级目录。

##### 3，注册拦截器。

- autowired等注入拦截器，拦截器才能享受spring的优势(拦截器内部autowired service)

##### 4，springMVC:最入门的理解就是把请求发到controller的工具

##### 5，实现退登功能。



>- 问题:检查token的方法有点重复，怎么办？
>- 解答:已经有拦截器了，每次都会检验前端带来的token，而且后端redis的token一般人也改不了，所以认为是标准正确的。
>- 有拦截器检查token，有controller检查有无用户
>- 在业务代码里，尽量用 `AdminHolder.getAdmin()` 直接拿用户，拿不到就抛异常。为了**保证业务代码的简洁性**。



##### 6，防重复登录，检查redis有无token。

>正好实现多设备登录的问题。

##### 7，修改密码接口

- 发现threadlocal不安全，重构了一下。
- 把key，401替换成了constant常量
- 401代表认证失败，适用于密码不对，token错误等情况。
- 运用mp书写set字段。运用lambdaUpdateWrapper.set方法。推荐lambda可以避免字段名错误。

##### 8，黑名单功能(不采用)

>很有必要，不然别人拿个以前的token直接冒充访问了。

##### 9，改成白名单功能

特别简单，就是检查redis有没有相应的数据。

但是有问题，两拦截器逻辑部分重复了。

##### 10，优化了两个拦截器的顺序，是最终保存的顺序，第二个保存

>控制台有报错输出，不要错过了，不然又要浪费大量时间

![77537444677](C:\Users\30580\AppData\Local\Temp\1775374446778.png)

就这个该死的东西，你能想到吗json后面跟了个逗号就不行了？？？？？？？？？？？？？？？？？？？？？？？
1，最后的逗号

2，地址后面不能有/

3，@RequestBody

##### 11,全局异常处理器

>似乎是有必要的，直接捕获全局异常。

trycatch然后打印，这是普通的报错误

~~~js
类-->@RestControllerAdvice-->@ExceptionHandler(Exception.class)
~~~

4/6

##### 前:

1,发现Element-plus的一个文件报了莫名的错，我认为不修改它更好，所以把ep的依赖卸载重装，事实证明有效。
2，如何发现是Element-plus的锅？查看文件所在包就行了，就能定位是什么错了。不要瞎点，不然很容易迷失。

3，后端redis的token过期了，但是前端的token还摆着呢。怎么办？发现401跳转到登录页，用全局拦截器。

4,真该死又卡主了，卡在了理解业务不透彻，应该在组件内部写那些按钮的，外部专心插槽就行了。

~~~js
mt-auto + flex-col
可以让按钮挤到底部。
~~~

5，依旧前端token怎么处理？

6，？？前端token怎么处理？过期了怎么办？过期没事，但是你一旦点击了请求，检测到你没有登录，就把你踹回登录界面。

7，优化路由守卫:优化token判断逻辑。之在请求之前都要检测一下token有效性，没效了直接跳到登录页面，终于完成了路由守卫。

8，hook封装loading逻辑。传入fn和fn的参数，loading是hook内部的loading，每次只有一个。这样有一个坏处，请求失败的话，我的promise.reject抛出了异常，导致loading.value=false被中止了。这种无论如何都要执行的就用try...finally了、

##### 后:

1，解决拦截器不返回result的痛点，浪费时间搞全局异常处理器还是搞不出来，在拦截器中抛异常是不可行的？

2，在gitignore上又耗了很久，但是是无用的，因为已经排除了。看看仓库，没有被提交啊，本来就被排除了，是吧。

![77545993795](C:\Users\30580\AppData\Local\Temp\1775459937955.png)

拦截器特供的返回result方法。注意要加入response.setContentType("application/json;charset=UTF-8").

不然就中文乱码

3，新方法，status设置200，data里的code设置不是200的，这样才对
![77546067158](C:\Users\30580\AppData\Local\Temp\1775460671585.png)

4，这下终于是把后端报错前端提示(包括拦截器的报错)的搞定了。



4/7

##### 前:

1,运用组合式api把修改密码，退登功能封装了起来。

2，打磨实现登录所有功能。

3，侧面菜单开发

- index是唯一标识
- template必须带上#title才能成为el-item的标题。

后:

1,遇到了以前重来没遇到过的超时问题，就是请求反复进拦截器，直到超时。

2，结果发现原来是请求路径写错了，就无限循环了。该死的。昨天忘记保存了，该死的该死的该死的该死的！

3，经过了一连串微调之后，改了后端一个状态码，就解决了旧密码的问题，关键就是调试太麻烦了，token飞来飞去的，希望以后能好点。

4/8

前:

1,一个卡了两个小时的bug（menu点击子菜单弹出错误）:结果是style没加scoped。。。。。。。。。该死的以后写这种涉及 UI 库深层样式覆盖（尤其是 Popper 弹出层）的代码，**务必加上 scoped**

2，有时候报错是有缓存，要强制刷新。

3，无论是组合式还是选项式，pinia的属性都需要storeToref来解构出结果，**不然就不是响应式**。

**完成**侧边栏折叠的功能。

4，又花了一个小时折腾动效。该死的，总结就是外层aside整一个宽度变换，里面el-menu也整一个，外层div加上动效。

5，动态渲染侧面菜单。

后:

##### 1,需求是获取当前用户能获取的菜单。

主菜单表:

- id int(not null,auto incr,unique)


- 主菜单名。varchar(255) (not null,unique,default “菜单”)
- 主菜单图标varchar(255) (not null default "齿轮")
- 主菜单路径varchar(255) (not null)
- 主菜单访问权限 int (not null,default "2"),0最低，1中等，2最高
- 创建时间，更新时间。
- 状态int (not null,default "1") 0禁用，1启动2，审核中，3，已删除
- 描述varchar(255) 

子菜单表:

- 子菜单名。varchar(255) (not null, default“子菜单”)
- 子菜单路径varchar(255) （not null）
- 子菜单访问权限 int (not null,default "2"),0最低，1中等，2最高。。。。
- 创建时间，更新时间。
- 状态int (not null,default "1")  0禁用，1启动2，审核中，3，已删除。。。
- 描述varchar(255) 


- 主菜单id int （not null）;

（order排序后面再说）

##### 2，建了典中典父子表

##### 3，思考存入threadlocal问题，结果是token解析id，获取用户。转换为不敏感用户再存入threadlocal，靠，我以前写过了，该死的。

##### 3.5什么时候用dto，什么时候vo，什么时候enity？

存储不敏感user的时候用dto吧

##### 4，对于菜单和子菜单，应该怎么做？

- 先选出没有子菜单的父菜单，需要加一个have_child字段吗？
- 如果不加，那么，搜索有孩子的菜单，我倒是会，多表联查。查出来放到结果类
- 但是没有孩子的菜单。该怎么办呢？

4/9

后:

1,或许应该把双表改成单表。

- 菜单名。varchar(255) (not null, default“子菜单”)
- 菜单路径varchar(255) （not null）
- 菜单访问权限 int (not null,default "2"),0最低，1中等，2最高。。。。
- 创建时间，更新时间。
- 状态int (not null,default "1")  0禁用，1启动2，审核中，3，已删除。。。
- 描述varchar(255) 


- 父菜单id int （not null 0代表是顶级，用check保证是0,1）;

2，包装类型需要兼容数据库的null值，实体类需要包装类型

3，建立返回VO。

4，最终思路是:

- 从threahlocal中取出用户role。
- wrapper，构建parent_id等于0、null的父菜单数据。
- foreach一下，每个数据先创建对应menuDTO，封装进去。
- 在父菜单里面，获取父id。
- 找到parentid不为0，null而且role要小于等于用户role，且parentid等于上述id的数据(子菜单)
- 封装到menuDTO。
- 创建新infoVO对象。
- 整个menuDTO加载到InfoVO里面。
- 返回前端需要的InfoVO。

5，query()链式查询是一个动作。语法糖

lambdaQueryWrapper()用于构建对象。

新表，新的菜单类，说明需要一个新的controller.

4.10

后:1,数据库里的desc是关键字，不能轻易用。加上``或者改成description
2，更改了token过期时间。复习xml获取配置，用类获取@ConfirguationProperities(prefix=".....")。

3,拼sql语句的时候，需要加括号的地方就用and加上。里面传入lambda表达式。

4，代码生成器的一个大坑:@RestController写成了@Controller，传统网页开发，返回的是网页，但我们需要的是数据

5，请求路径要加api理由:前后端域名很像，前端静态有一个/getinfo页面，后面有个/getinfo请求，这不乱套了？所以加上api表示是后端的

6,@RestControllerAdvice不用rest报错，内存就会疯涨。

前：1,完成头像的设置，终于动态的把头像名字渲染了，最好定义在下面，好修改。

4/11

前:

1，完成动态路由，有点难，遇到了无数的bug

2，重构menus存储位置，存储到了pinia，以后还是多存储到pina吧；

3，把console的垃圾报错搞一搞，更清晰

4，发现应该获取了，但是没有数据，还有api，就要考虑是不是await没有阻塞进去。

5，路由标签页的添加

- 基本样式
- 点击激活。
- 添加
- 切换激活。如何实现:用所谓的路由守卫，在路径更新之前

(onbeforeRouteUpdate是在组件内部的微调员，onbeforeEach检查token什么的，性能开销更小。)

这是监听事件，有改变就触发，onMounted用于监听器，图表渲染什么的。别卵用。

6，下拉菜单和关闭功能，封装

7，优化点击速度，就是把getInfo右移了

8，keep-alive缓存原理

# 4/12

##### 1，keep-alive项目运用。

~~~js
router-view里面包keep-alive组件。再加入component手动渲染。
~~~

##### 2，transition动效

~~~js
animate.css动效。
就是加入transition标签。然后指定name再加入css。比如fade-enter-from{opacity:0}
~~~

##### 3,统计面板。

当前任务:搭建四个card用于展示数据。

##### 4，setup的作用：把data，method收拢在一个地方。

关键是定义响应式状态。并且暴露给模版。

5,了解了逻辑写一边，展示写一边，和props和emit真正含义用途。

5.5，完成后台第三方添加猫和用户端切换猫源。

6，遇到304检查路径有没有api

后:1,或取到统计数据。

目标:1,cards:珍惜猫数量，猫数量，用户量，新增用户量。

2，快捷图标。

3，新增用户统计。

4，珍惜猫状态:卖光了的，总量的，在卖的。

5，普通猫:总量的，无家可归的，热门的。

+++修改用户端，改成从自己的接口获取猫，而不是从第三方接口。

++++++修改管理端，这边调用第三方接口，用于获得猫，哈哈哈。

当前任务:

1，ep重构布局。

2,制作管理员添加猫的页面(完成，还需要后端的接口)

2.1设计表(catAdmin)。

~~~js
1,id
2,name
3,cat_id
4,image_url
5,create_time
6,update_time
~~~

2.2参数:

~~~txt
1,name
2,image_url
~~~

3,多抓几只猫，然后用户调用后端接口，获得猫。

3.1需要随机方法，随机取一只猫。这也不是很好弄。

~~~js
1，查出猫总数。
2，生成随机数做成页码(想到Math.random()api，不要总是问ai！0.0-1.0*100，1-100，)
3，limit n,m
就是说，从偏移n开始，取m条数据。
~~~

什么玩意，基本类型的话，生成的@Data就是isDeleted(),以后写这个实体类不要定义基本类型。

Long和long分清楚，tmd，Long也是包装类型。。。。。。。。。

# 4/13项目开发

# 1，解决服务直接调用的问题----------->restTemplate

不妙。token问题。用户端--->管理发请求--->token带Bearer！(拦截器就不拦截了，这是一个公共接口不用登录)

### 2， Java 的**泛型擦除**机制，常见于服务之间互发请求

运行时，JVM无法识别<T>是什么，jackson反序列只能转换为map

![77604760385](C:\Users\30580\AppData\Local\Temp\1776047603857.png)

### 3，发现没必要互相发，只需要在用户后端查数据库表就行了。真奇怪啊。

发现需要从管理端获取猫的总数才可以。

最后是靠加了张admincatVO表才可以实现，单体架构，现在基本上就是秒级获取了，很快很快。

(另外，发现了pixpin可以录制动图，太厉害了)

### 4，提交不上github，把校园网关了就交上去了。

### 5，**继续搞主线**。到哪了？是成功有了猫表。顺便改造了用户端。下一个就是珍惜猫表了。

##### 5.2珍惜猫表怎么设计？

- id
- name
- cat_id
- image_url
- create_by
- is_deleted
- status
- create_time,update_time
- store库存

change和modify:change可以改名，但必须指定

目标:获取到统计数据。

###### 后端;1,接口，无参。计算四个数据

返回珍惜猫数量，猫数量，用户量，新增用户量

### 5.3swagger附属统计接口

引依赖，yml配置(启动knife4j)，配置类(基本内容)。

拦截器背大锅，需要排除knife4j所有路径(指yml配置的所有路径)才行

### 5.4用户数据库设计

- id
- username
- password
- image_url
- last_login_in
- phone
- create_time,update_time
- status----0封禁1正常
- isDeleted  0没有1有

### 5.5统计之今日用户怎么处理？获得今日的0,24点？!

简单方法:now()转换为LocalDate，只有日期。在用localDAte的api，atStartOfDay()，再加一天，在算出atStartOfDay(),这就是LocalDAteTime了，最后再比较。



# 4.14

### 后:1，继续debug统计接口。

##### 1.1. 打开 SQL 日志，看它到底执行了什么

##### 1.2查看sql下面的参数，发现错误

##### 1.3测试完毕

### 2,前端要求的后端功能:

##### 2.1，封装totalVO,里面一个集合<CardVO>

##### 2.2封装cardVO，里面是

```java
    private String title;
    private Integer number;
    private String unit;
    private String subTitle;
    private Integer subNumber;
    private String subUnit;
```

##### 2.3调整后端逻辑(controller够用吗?)。

###### 2.3.1title,subTitle和subUnit，unit都要设置Constant。

###### 2.3.2每次封装一张卡片，比如封装title，统计出的数字，单位，子title，子数字，子单位。

封装函数，可以用全参构造代替

###### 2.3.3封装到totalVO的集合中。

###### 2.3.4返回totalVO

💡小想法:可以衍生出用户上传猫，加入民间猫池，供大家捕获。像咸鱼那样，有官方(后台管理系统添加)，还有民间上传(用户自己上传)，统计猫的数量就是在统计这些加起来猫的数量，上传官方审核，后台管理系统事情就多了，挺有意思。

##### 思考:为什么要用包装类型?

自动拆箱会导致null指针异常。实体类使用包装:

1,为了避免缺失值导致的null，毕竟int传入null是0。2，因为后面处理以后很多都靠集合，你传个基本类型后面什么泛型操作会导致很麻烦的情况。

###### 2.3.5决定以cards为正常的可用的，资本。以下面的两条为真正的带不正常数据的总数。



### 前:1,前端卡片功能。

##### 1.1发现后端是完全错误的。

##### 1.2修改接口后，用v-for循环出col，渲染出卡片，注意获得的数据有没有包一层数组。

##### 1.3完成卡片的开发。el-tag,card,layout,

### 2，快速导航功能

##### 2.1教程的太固定了，我想要搞一个用户访问的越多，导航card排名越靠前。

这需要什么技术？redis吗存储一个用户访问一个功能的次数。

cat:normalcat :用户id次数这样吗。

##### 2.2分析菜单列表都应该有什么功能

~~~js
猫管理:普通猫、珍惜猫
用户管理:用户管理
管理员管理:管理员管理
系统设置:主题设置什么的
日志管理:用户操作日志（这也太多了），管理员操作日志
~~~

##### 2.3生成数据(ai)

~~~js

~~~

# 4/15

### 后:1设计第二个统计数据接口

###### 1.1目标:根据type，week或者month

- week:如果是一周，要统计用户createtime在第一天，第二天，第三天。。。。的总人数
- 再封装成图表需要的x轴，y轴数据。
- month:同理，但是数据就多了

###### 1.2古法低效率思路

1. 先获得基本的现在时间。还有新的x轴集合，还有y轴集合。
2. 转换为localdate。
3. 如果是week
4. for循环7次，0-----6.
5. localdate.plusdays转化为那一天开始的时间和结束的时间。
6. 在数据库里面筛选。出那一天的人数。
7. 加入x，y轴集合。
8. 返回给前端。

##### 1.3推荐方案

1. 先获得基本的现在时间。还有新的x轴集合，还有y轴集合。

2. 转换为localdate。

3. 如果是week

4. ~~~sql
   select DATE_FORMAT(create_time,"%Y-%m-%d") as day,count(*) as count from user where 开始时间，结束时间 group by day
   # 贴标签，createTime字段附上只有日期的值，groupby前面毕竟只能有什么自己本身这个字段还有什么count。
   那就分好组了。返回的就是我们的Map了。直接日期和时间
   ~~~

5. ~~~java
   //构建条件。
   selectMaps(new LambdaQueryWrapper().select("DATE_FORMAT(create_time,"%Y-%m-%d") as day","count(*) as count").lt().ge().group(DATE_FORMAT(create_time,"%Y-%m-%d"))
   ~~~

6. 在数据库里面筛选。出现了list包裹map。

7. map直接处理不好，所以需要转换为对象，这时候就需要定义一个实体类。或者你知道。

8. 转换为json（JSONUtil.toJSONString），再转换为DTO对象

9. 想把返回的数据办法加入x，y轴集合。

10. 返回给前端。

1.4所谓的map不是你想的那样你以为数据库查到的是2024-3-12---3，实际上是col名+值啊，比如说day--->2024-4-12，count--->3这样的。

### 前:1,快捷菜单功能(后续动态拓展)

### 2，研究封装理念，可以slot插槽植入数据.

### 3,图表功能。

记得要看黄色的报错。一看黄色的报错，就知道问题出在哪里了，没有加setup。。

##### 4,近一周近一个月切换功能。

如何才能不设置长宽，不用margin实现分开呢？

方法一：使用 `gap` 属性（最推荐）

# 4/16

### 后:1,图表数据排列优化

###### 1.1发现数据排序不对，于是orderBydesc降序排列，发现如果没有人在一天注册就不会渲染出来。怎么办？

### 2，indexCard接口。

###### 2.1思路

- 没有参数。
- 获取两条数据。
- 一条就写猫一条珍惜猫。
- 猫:总数。上架数，无家可归数，下架数。
- 珍惜:总数。在卖上架数。卖完了的，闲置可用的。
- 猫:
- 1,查总量(没被删的)
- 2,上架的
- 3,无家可归的(ownedBy)
- 4,下架的
- 珍惜猫:
- 1,总量
- 2，上架的
- 3，卖光的
- 4，下架的
- 封装成result类的数据里
- 封装一个detailcard，里面是一个数字，一个label标题。
- 返回给前端

##### 思考:为什么要用包装类？

因为数据安全没得说。private无法直接修改，必须setter。

因为封装着方法，这样你可以快速的修改方法来修改逻辑，而不是在外面修改，特别麻烦。

因为可以降低模块之间的耦合度。

### 3，修改数据库

###### 3.1加入拥有者数量字段，显示有多少人用。

##### 3.2删除逻辑:下架删除。

### 4，sql错误调试，查生成的sql语句，放到sql里到底能不能执行成功。

原来wrapper传递不是副本，你这样一直加条件，是错误的。生成sql语句可以看见。

正确的调试方法才能成功，你不看生成sql在那里瞎想，只能消耗脑力

### 前:1,indexCard功能。

1.1封装组件，传递数据(数组包对象)

## 4/17捣鼓一天就为了自动化加入rule，而不是手动维护。

>以后你接口没了什么的，数据库会跟着变，不用你手动去修改数据库数据了

#### 后:1,设法获取rulesnames数据。

#### 2，自己造注解

###### 2.1`@Target(ElementType.METHOD)`

Targte注解是目标，说明这个注解因该用在哪个地方，你可以指定Method方法，只能用在方法上。

###### 2.2retention注解

保留多久？指定runtime就是在运行期间还保留标签

###### 2.3保留参数

里面的参数就是需要传递的参数。

###### 3，syncScanner方法专门用于扫描到注解传入的数据。

3.1获取类

3.2获取方法

3.3挨个检查

3.4有就调用service更新数据库。

###### 4,还有一个service方法，用于保存扫描到的list到数据库

4.1遍历list

4.2依据code找数据库

4.3有就更像

4.4没有就插入。

>```
>implements是接口，extends是继承类
>```

#### 5，设置了getMappingObject方法，返回obejct用于获取值

但是Object太局限，可以转化为

```
public String getMethodPath(Method method) {
    if (method.isAnnotationPresent(PostMapping.class)) {
        return method.getAnnotation(PostMapping.class).value()[0];
    } else if (method.isAnnotationPresent(GetMapping.class)) {
        return method.getAnnotation(GetMapping.class).value()[0];
    } else if (method.isAnnotationPresent(PutMapping.class)) {
        return method.getAnnotation(PutMapping.class).value()[0];
    } else if (method.isAnnotationPresent(DeleteMapping.class)) {
        return method.getAnnotation(DeleteMapping.class).value()[0];
    } else if (method.isAnnotationPresent(RequestMapping.class)) {
        return method.getAnnotation(RequestMapping.class).value()[0];
    }
    return "";
}
```

Object变化为特定的String，好多了。

总结:花了半天以上时间，捣鼓了自动化加入rule，运用注解，跟荧光笔一样，指定rulename和level，然后定义一个自动扫描类applicationRunner，扫描注解，然后自定义一个service狠狠的插入更新数据库。中途有一堆新api

# 4.18

后:

## 前:1,directives全局指令

#### 1.1定义全局指令js

###### 1.1.1想定义指令？参照格式

~~~js
{
    mounted(){
           check()；
    },
    onupdate(){
           check(); 
    }
}
~~~

导出这个方法。

#### 1.2注册使用指令

1.2.1直接在mainjs，app.directive（）方法，传入名称和你定义的指令方法

>另外，以下是局部的，单组件的使用方法

> ![77647875629](C:\Users\30580\AppData\Local\Temp\1776478756295.png)

得出了结论，ai不可信，必须指出用最新的方法，最新的技术，才能正确使用

1.2.2小bug，结果是退出的时候把路径也弄没了。

~~~js
没有解决，不知道为什么会route，看来是我杂合的问题了，文件执行顺序都不知道。
~~~

1.2.3小bug，看来是登录的时候没有rules，和menus保存信息导致的。

~~~js
因为登录的时候已经获取过信息了，再获取一次可不好。
~~~

1.2.4token && !hasGetInfo原来是这样，因为这个会话获得过信息了，根本不会再次获取，那就登录手动获取一次

~~~js
结果又遇到了执行问题，又是什么router报错。
~~~

12.5我想实现一个退出登录，方法退出的功能，结果杂合一起了，这时候你就需要pinia了。加入isgetINfo

~~~js
用pinia存储isGetInfo信息，不要随便导出，pinia就是为了这个而生的。
~~~

12.6又是bug，tab栏没有正确清除。原因是一刷新，tablist是从cookie获取的数据，cookie没处理自然会获取到错误的tab信息。

~~~js
就是这样，tabList存储到了cookie中。
~~~

### 2,图片管理功能

###### 2.1setup语法糖的作用

- 变量，函数自动暴露给模版
- await直接使用
- defineProps,defineEmit简化
- 导入的组件自动注册

##### 2.2开发基本布局。

calc(100vh - 160px)真好用

这个基本布局弄了我两个小时。。。。。

![77651261476](C:\Users\30580\AppData\Local\Temp\1776512614767.png)

el-container包着el-header，用calc固定大小。
![77651275228](C:\Users\30580\AppData\Local\Temp\1776512752282.png)

然后el-aside注意要relative里面包着absoulte，然后呢，问题就是容器的大小就是你的侧边和main的大小高度，你要注意了，然后用绝对定位，定位文字所在部分。绝对定位没这么难，你还忽略了bottom-0就是最底面这一说。

~~~js
先是directives全局指令，你要自定义指令，前端方法就是先定义条件(有mounted还有onupdate，触发之后该干什么)，然后是去mainjs里面app.directive（名称,条件）。就可以使用指令了。

那么，当v-permission指令触发的时候，就触发检查permission方法，首先将传入的值和元素，值和pinia里面的数组比对，如果有，就把元素的style的display改成none。

接下来就是图片管理了，图片管理可以复用图片，同一地址，而不用每次都从本地上传了。
~~~

# 4/19

前

1，我觉得是教程的问题。他的样式和我的不一样，所以不好用，所以只能做参考了。不然一直没有章法。

2，布局了列表item小组件。

3，获取后端接口渲染数据

4，台风css有时候失效。建议选取普通style，特别是margin类的。

后

#### 1，开始开发image列表接口。。。典中典。

练一下分页吧。

分类表

- id
- name
- createupdate
- order(决定显示顺序，用于列表数据排序）
- countimage(多少张图片，便于前端显示)

只能有一个自动递增的字段咯。

### 2，confituation注解的作用。

@Configuration 

注解的类可以包含多个 @Bean 方法，这些方法<u>返回的对象</u>会被 Spring 容器管理为 Bean。

*@Bean* 注解用于方法上，表示该方法返回的对象会被注册为一个 Bean，并交由 Spring 容器管理。它常用于将第三方库的类实例化为 Bean。

#### 3，使用mp分页插件

###### 3.1先引入jsqlParser依赖。

###### 3.2在拦截器注册这份依赖

#### 3.3使用

###### 3.3.1创建分页对象

~~~java
Page<User> page=new Page<>(1,10);
//运用我们的selectPage方法。
userMapper.selectPage(page,"条件");
~~~

##### 3.3.2好久没用路径参数了，别忘了

![77658400876](C:\Users\30580\AppData\Local\Temp\1776584008763.png)

后面要加上路径，不然报resource错的

##### 3.3.3拷贝成不敏感数据

运用map函数再tolist，lambda没有简化。

> controller和restcontroller。区别在于加不加requestBody，加这个接口返回的就是json，不加返回的页面视图。

#### 4,一下解决了代码生成器的两个错误

再策略配置中启动了mapperBuilder()的mapeprannoation注解。并子啊controllerBuilder（）启用了rest风格。。

##### 4.19总结:今天就完成了两个地方，一个后端获取菜单接口(学会了ep分页)，一个前端分页渲染出数据(pagination)。

台风css有时候margin会失效，推荐直接使用普通的。

# 4.20

##### 前:

###### 1，用drawer制作一个添加分类名称。

1.1调用drawer，传入title。如何让drawer显示？调用里面的open方法

1.2因为按钮在另外一个组件，所以需要defineExpose暴露方法。

1.3组件使用这个方法即可。

1.4被lang=ts坑了，我加他干啥。。

#### 后：

##### 1，要完成一个传入page，增加新分类。关键就是这个order参数。

##### 2，后端接收到后。查数据库，先把等于这个order的查出来，没有就直接插入，有的话难道要把大于这个次序的order全部加一？不合理，怎么办？

ai说用小数。好主意。

###### 2.1先把等于这个order的查出来，没有就直接插入，有的话就查询下一条数据，获取到order，计算出新的order，插入。

###### 2.2加入@Transactional

>我用的已经不是restful风格了，是RPC风格，基本只有getpost，然后以路径名称标识资源。

##### 3，简化接口逻辑



4.21

后:

### 1,完成后端修改接口和查询接口优化。

> 27min

###### 2,分类删除功能

正好练习一下伪删除--->ai提供了新功能--->mp的逻辑删除

1，加入isDeleted tinyint

2，加入TableLogic注解

3，配置logicdleete，在yml中

注意，你不要手动加入isDeleted为0，**你不需要写这个条件，MyBatis-Plus 会自动帮你加上**

而且，标签方法要加上delete。

好的。浪费一小时，就因为这个该死的selectPage我记错了，继承了selectList。。。他还不报错，ai不开深度也检测不出来？？？？？

前:

### 1,分类修改界面

> 25min

思路:

~~~js
//点击了编辑电钮。
  //1,获取到要更改的id.
  //2,显示drawer,改标题
  //3,输入
  //4,点击提交，调用修改方法。
  //5,提示成功，关闭表单
~~~

##### 因该刷新的啊，难道是缓存？原来是异步问题。。。。。。数据不一致。。不就是异步问题1吗？？？

# 4.22

>接近尾声，必须提速

##### 前:1,完成图片展示功能

- main组件定义方法，获取到数据，暴露给父组件
- 完成左组件选择方法，选择就通知父组件change了
- 父组件change方法改变main的数据，重新获取就行了。

##### 2，排查显示错误。

我怎么没想到后端重构了呢？

我还以为是什么异步问题。这种突然的bug联想你刚刚做了什么。(git)

##### 3,又被数据显示bug卡主了。

结果是该死的await。script里面还是不要写await好了，不然的话属性都挂载了，你还在请求，当然没有

##### 4，重命名elmessageBOX提示框应用

##### 5，删除功能。

### 🏆挑战任务

###### 22分钟之内完成修改图片(重命名和删除)接口(后端)

奖励:1,5元之内的小饮品+dance

✅️完成。14分钟。

但距离企业要求的"能独立负责模块开发"还有差距。建议重点提升**代码健壮性**和**安全规范**。所以时间真的不多了。你还需要重点提升代码规范和健壮性

#### 后:1,图片获取接口

#### 2，图片修改接口

#### 3，图片重命名接口

# 4、23

#### 后:1,图片上传实现思路。

1，确定前端目录

2，new file一个前端目录，确保dir存在，不然就创建

3，用时间戳+文件名生成新唯一文件名。

4，transferTo上传到这个目录

5，不然就返回

##### 2，决定跳过麻烦的oss，先赶进度，再30号之前要做完的。

##### 3，加入缺失的注解

##### 4，公告crud

#### 前:

##### 1,图库全部完成

##### 2，公告删除功能

表格--> template #default="scope" -->才能使用scope.row.id

##### 3，公告修改功能

运用计算属性把drawertitle算出来。

然后提交看到底是currentNoticeId咯。

##### 4，管理员接口

超级管理员1管普通管理员0，顶级管理员2级管理所有的。

发过来请求查询管理员表。

问题是动态拼条件，前端可能没有那个条件

###### 解法是:eq,lt等传入第一个参数布尔值，通过true or false，判断要不要拼贴。

get请求用params时候后端解析不到json哦。

表格当前行拿不到，这可不行，用 template #default="scope"吧。

# 4.24

> 都这个时候了，必须冲刺了，今天完成管理员模块吧。

### 前:

##### 1,管理员搜索功能

如何左右布局？只需要layout分两个:span=8就行了。offset调整为8，偏移8列。接着justify-end

##### 2，查找路径传递

对象传入，用{params}转化为查询参数，同时后端的@requestParam会自动把查询参数封装为DTO对象咯。

##### 3，经典修改管理员状态

表单元素自带:loading属性，你需要传入false or true;

$event它是获取组件触发事件时携带数据的标准方式

:disabled:"row.super==1"，还有操作也是禁用的

##### 4，新增修改删除管理员接口

##### 5，调试了一个半小时的bug

表单无法输入，就是v-model没有绑定上，结果是tmd form重名了，被另一个ref也使用了，model当然绑定不上咯

###### 6，select看起来是什么什么员，实际上就是1,2,3,4的值罢了。

##### 7，锻炼了计算属性的运用，选择合适的策略，透过不合理的字段，计算出了roleCode对应的role。

### 🏆挑战任务2

###### 22分钟之内完成新增修改删除管理员前后端接口

奖励:2元小蛋筒或者3\4元小饮料。

❌️挑战失败，前端太费时间了，后端倒还好。

##### 4.1痛点在于，roles的配置，我忽略掉了roles

4.2写roles，后管基本不需要忽略敏感信息的，不需要dto化

4.3建立表role

~~~sql
id
name
is_deleted 逻辑删除
create_time
update_time
status 0启用，1禁用
~~~

#### 5，重构业务咯，真该死啊，一堆杂七杂八的都要改过去。

### 为什么需要多表联查

因为防止数据冗余，管理员表和角色表分离。

RBAC权限系统要求角色用户分离。

别心急，**<u>一步一步来</u>**。知道吗？

5.1重构admin数据库表，加入roleCode字段。

>以后字段尽量简明知意。
>
>有点矛盾的就不要用。
>
>以后痛苦重构你就知道为什么要这样做了
>
>比如role应该更名为permission_level

admin实体类也改。

查询admin列表的时候要多表联查了。这是一个问题

5.2设计角色有哪些。

>猫审核(制作上传猫,审核民间上传的猫)
>
>运营(稀有猫怎么运营，主页推销图片管理，公告设置)
>
>超级管理员(level1)
>
>root管理员（level2）

加入role

5.3把admin表加入roleCode重构(数据层面)

5.4重构前端显示这个role名称。

5.5把菜单的普通管理员改掉改成成员列表

###### 应该能完成基本的列表查询了

5.4然后把找到的role存入前段的roles列表，大概就可以在前端使用了。

## 6，mp多表联查

##### 6.1leftJoin左联，有没有dept都会被显示。

##### 6.2innerJoin内联有数据的才会被显示。

~~~sql
select a r.name roleName from admin a left join role r
on a.roleCode=r.id
,
select a r.name roleName from admin a inner join role r on a.roleCode=r.id
~~~

#### 6.3mp集成多表联查

>总算完成了

6.3.1引入mp-join依赖

6.3.2配置

6.3.3使用

继承MPJBASEMAPPER即可，既可以使用

~~~java
MPJlambdaWrapper.selectAll(左).select(右).leftJoin(右，左右相等字段)
~~~

多表联查中间变量dto

#### 7，实体类套实体类逻辑

该死的，records字段里面包着AdminVO才对，里面要用一个RoleVO属性才行，重构吧。

怎么办分页和这个多表适配问题。

我去直接提供了现成的selectJoinPage方法！太棒了！但是，你要手动指定返回的类型是AdminVO才行。

问题出在

~~~java
adminMapper
.selectJoinPage(adminPage,AdminDTO.class,adminLeftJoinRoleWrapper);
那个page也要指定为你最后想要的样子，wrapper自家人没事，中间指定要返回的class，前面是page不是自家人，必须指定返回什么。
~~~

嗯，怎么办呢？

记住了like(条件，字段，值)

*再这样下去项目是开发不完的。*

##### 8，有一个痛点

传入的是roleCode，你需要查表获取他的role？？？？不可能。

# 4.25

>再加速！再加速！

#### 前

在 Vue 模板中，必须使用冒号 : 绑定动态值才能传入布尔值：

##### 1,完成了烦人的checkbox，change函数要传入item

##### 2，该死的，js的数组是push方法，我急成add了

3，过度依赖调试，不自己思考，不自己写完流程

所以你才做的这么慢

##### 4，v-model拿到chooseImage组件。

##### 5，封装js

封装api

封装js的时候记得要用里面的响应式对象，**而不是把自己的传进去**，这是不对的。而且最好是写一部分检查一部分，不然太慢了。

~~~js
    searchForm = reactive({ ...searchFormParam });
~~~

# 4/26

>太慢，太慢！

##### 前:1,权限树形。这该怎么办呢。。。

关键样式被max-width阴了。

##### 后:1，权限树接口

1.1想法1

menu和permisison有很大的重合。不如在permission上设计一个menu_id，多表联查，查出menu套permission，里面包含了所有接口，然后就是菜单也可以设置关闭还是打开。

1.1.1

bug,menuId是什么？你怎么扫描到并且插入？组件再加一个？不可能啊。

前端传递api路径，请求头插入api路径，后端查找相应菜单。

大问题，无法获得menu_id。

ai回答:这是典型的**权限-菜单分离架构**问题，需要通过**逻辑关联**而非物理外键来解决。

>依据我的验证，这并不是一个好主意。

1.1.2

mp需要实体类，这个rule功能要什么实体类？

1.1.3

权限管理意义在于，顶级管理员可以控制菜单和对应接口的权限，看看能不能访问。

1.1.4

放弃，因为他那个权限是需要自己调配的，而我是自动扫描的。

所以我只需要做那个，不对，管理员可以调控这个接口的配置，算了，算了。 

#### 2，备份数据库

在企业怎么办？悲。

#### 3，开发剩余权限菜单功能，完善优化

还得是谷歌浏览器啊，一下就把错误显示出来了

# 4.27

前:

##### 1,制作图标选择组件

###### 1.1v-model双向绑定

顶层原理是:modelValue，子组件emit和props定义好update:modelValue就行。

###### 1.2打开drawer显示bug修复。

把那个选择做成只有新增时才显示。

再传入doafterOpenDrawer函数。

> 小细节:定义函数的时候用row={}意思是没传的时候默认null,使用函数的时候row={}意为把row赋值为{}，意思完全不一样，反复定义了

###### 1.3修复冒泡状态栏小细节

后:

1,快速开发接口

### 🏆挑战任务3

###### 25分钟之内完成role前后端所有功能。

奖励:2元小蛋筒或者3\4元小饮料。

注意你的不足：

1. **花一周时间补Java基础**
   - 重点看：基本类型vs包装类型、泛型擦除（你日志里提到了）、接口vs抽象类、HTTP协议（GET/POST传参区别）
   - 推荐：《阿里巴巴Java开发手册》（网上有免费PDF），看完你就知道为什么字段命名、常量定义、异常处理要规范。
2. **把这次项目整理成"脚手架"**
   - 你花了大量时间在配JWT、配拦截器、配跨域。把这些抽成一个**基础模板项目**（Base Admin Template），下次做新项目直接复制，能省50%时间。
3. **前端补工程化**
   - 学一下pnpm、学一下`.env`环境变量配置、学一下Vite的路径别名。
   - 把VueUse、Element Plus的版本锁定，避免缓存问题。
4. **写一个完整的README**
   - 把你遇到的坑（ThreadLocal、多表联查、部署步骤）写成文档。这是企业很看重的能力，也能帮你复习。

# 4/28

##### 1，项目部署。

1.1linux项目部署之前端

1，nginx的html目录

2，conf配置文件书写

1.2linux项目部署之后端

前端：

1，把前端dist文件引入nginx的frontend文件中。

2，后端jar包放到后端target/中

# 4/29

>前置条件：jdk17下载好，nginx下载好



捣鼓项目部署

### 1,原来是我配置的jdk出问题了。

那个什么openjdk不存在。换成另一个jdk镜像就行了。

##### openjdk:17-jre-slim是吧，我记住你了

### 2,测试页面功能部署是否正常

你这个愚蠢的程序员，访问虚拟机用localhost是吧。

###### 2.1链接不上后端服务器

大概率是端口被占用了，这时候需要看日志了(docker logs <容器名>)

发现是我的yml配置文件错了，哪来的OCM？你的数据库是mysql对吧？访问必须要改成192.168.100.128

###### 2.2需要把数据库也搬到mysql上。

怎么做？生成的sql文件跑到虚拟机执行就行了。怎么做？ 

###### 2.2.1传入sql文件。

###### 2.2.2docker cp 把文件复制到容器内

docker cp 原路径 容器:容器路径

docker exec -it 容器id 

ls -la查看目录

###### 2.2.3docker exec -it 容器id bash，进去容器内部

it命令是干嘛用的？

###### 2.2.4进入mysql服务

use选择数据库。

source **当前 Shell 环境**中执行指定文件的命令

###### 2.3每次有错误都要改yml，重新打包，重新编译，是吗？

可以启动的时候搞一搞参数传入地址。
![77742784160](C:\Users\30580\AppData\Local\Temp\1777427841607.png)

###### 2.4启动springboot项目。

启动失败，jdbc错误，有没有可能是密码用户没传？

启动成功！测试一下功能。

###### 2.4.1，502 Bad Gateway

网关错误？网关不是控制前端请求往哪发的吗？

服务器端错误？奇怪了，明明看起来是正常的。

~~~js
不是后端的错误，是nginx的错误。nginx和springboot通信失败了。
怎么办？不是瞎搞，是看日志。

tail -n 50 /var/log/nginx/error.log | grep "502"
没有错误，于是
curl -v http://localhost:8080/health
发现后端错误了
卡住了，這該怎麽辦？
~~~

>补充，curl是用来和服务器交互的， -v用于查看调试信息

我先确认是不是服务器的问题，这就是排错经验啊。

是sb的错误。

请求压根没被 Spring Boot 的业务逻辑接收到

好了，结果是后端**运行在8081端口上的，结果你映射到了8080上**，怎么会有用呢对吧？

而且nginx是在容器内通信，直接容器名+端口就行

使用mysql服务语句要加分号哦。

终于进去了！服务完全打通！

![77745169686](C:\Users\30580\AppData\Local\Temp\1777451696863.png)

另一个项目。打包，上传，写compose文件，注意宿主机端口被占用了哦，而且mysql不用写，比较已经启动了，访问宿主机就行了，会映射到容器文件的。

而且nginx监听的是80端口不是90端口，容器内是80嘛。
部署完成！

![77745341594](C:\Users\30580\AppData\Local\Temp\1777453415946.png)

学会了就很快了。

###### 2.5最简版本会了，该加强了。

首先你总要写一个dockerfile，这是用来保存镜像，防止你一直手动传jar包。

这样镜像就是自己构建的了。

加入mysql文件夹，里面是init和data。

还有.env存储密码等敏感信息。

# 4/30

### 1,真正的部署

导入文件之后，编写前后端的Dockerfile。

from 镜像是什么。

指定工作目录为app

copy 当前目录的(target/jar)  内部目录

内存限制

暴露端口

entrypoint执行

### 2，dockerfile意义？

###### 2.1，指定好jdk镜像，防止服务器自带的jdk报错(每个容器都有独立的jdk)

###### 2.2配合restart: unless-stopped保证jar一直在动

###### 2.3指定容器的端口

###### 2.4负责指挥，在那个网络，镜像们在一起。

你自己挂载volumes，不累吗？

###### 2.5注意了

mysql里面init和data里面，data为空，init(sql)才能初始化

###### 2.6排错依赖日志

不是脑洞挖出来的

日志日志日志

一般会标出一个ERROR，你用grep呗。

### 3，排错说什么数据库有数据了

/var/lib/mysql 是 MySQL 容器内部的一个目录，你无法直接在宿主机上找到这个路径

但是已经映射到了你的宿主机目录了

具体解决我也忘记了，好像是路径错误？

# 5.1

为什么访问总是看到nginx默认页？说明nginx配置失效(映射)

### 1，半场开个香槟，因为你在dockercompose.yml又映射了一个conf文件

于是把nginx那部分删掉了

### 2，然后向外暴露了一个端口80

就成功了哈哈哈哈哈哈哈哈。

### 3，再次练习真正的部署

##### 3.1先构建好目录。

frontend

backend

mysql

.env存储密码、redis密码

docker-compose.yml编排文件。

网课:

###### 3.1.1compose意义

如果有很多容器，而且容器有顺序，难道要一个个部署吗？

![77763152673](C:\Users\30580\AppData\Local\Temp\1777631526733.png)

###### 3.1.2容器 

> 源于镜像

**镜像 (Image)** 就是**兵马俑的模具**；
**容器 (Container)** 就是**脱模出来的兵马俑**。

dockerfile用于,就是将镜像脱模为容器！！

后端jdk镜像，运行jar包

前端。。。nginx镜像？运用html和conf文件

###### 3.1.3编排。

mysql-init-data-conf

后端没有现成镜像因此需要手动构建 build: ./Docker

depends_on: - mysql

网络问题

###### 3.1.4dockerfile作用

Dockerfile 就是**制作 Docker 镜像**的“自动化脚本”或“施工图纸

##### 3.2compose启动一下

重名等问题解决。

还有一点

###### 3.2.1dockerfile和dockercompose问题。

dockerfile用来构建容器（搭房子）

*此时镜像就是个空壳子，你的配置(家具)还没有挂载上去

dockercompose的volumes用于运行容器。(换家具)

>为什么要compose呢？因为这样可以热更新，你指定volumes挂载之后，就可以reload不用重启容器，直接改挂载配置~

###### 3.2.2为什么写了valuesvolumes还要写copy。

1，copy保证了镜像的兜底机制，如果没有外部挂载，可以按照默认值挂载

2，所以需要在外面再写一份配置比如nginx.conf。

###### 3.2.3found character that cannot start any token

 這個報錯意思是出现了不明字符比如tap，千万不要出现tap。。。这是yaml的规定口牙

##### 3.3部署完成，这下ok了

我已经学会了用docker-compose快速部署项目了，一次启动多个容器，指定好数据卷挂载，指定好容器名字，端口暴露，网络，构建镜像方式，重启和顺序问题。



=======================开发基本完成======================