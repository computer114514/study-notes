2026/2/10
	抓猫web项目
需求:1,不同用户登录，有各自的数据
2，抓猫存入数据库


1，加工前端，加入axios统一器，向后端发送各种请求
2，架构设计问题:调用第三方api？后端中转；后者，因为你获取的到要加入属性，名字什么的，
而不是第三方的一个图片链接，而且要存储到DB前端不能操作DB，后者大大扩展了功能。
3，创建yml配置信息，加入插件，创建sb项目
4,要配置mysql数据库了。

2/11

1，完成数据库的建立与配置
2，架构：前端获取链接预览，确定save再调用后端接口传入链接就行
3,完成前端获取图片预览。
4，最终否决重新写第三方api，因为结果和我的不一致，所以直接写好了
5，调整前端的异步请求逻辑------------异步请求返回promise，你对他json()也要异步，不然是未定义。
6，复习了父子组件通信，子组件要传递数据需要emit父亲的方法，好奇怪
8，抓取接口开发完成
9，定义了result类。
10，result类忘了@Data，导致返回调用result.success()的时候没有任何数据，你返回的实际上是个对象，怎么转换为
JSON？jackson反序列化，jvm用这个将对象转换为json，jackson通过getter获取数据。
11，父组件传数据，：data，子组件defineProps为props，再props.name这样使用,
12，bug:一定要注意，传入的json的格式，名称和映射的名称一不一致，不然会卡主
13，为什么前后端可以联调？因为前端放松轻松到后端服务器了，你没有配置后端服务器，算什么话？
14，必须要server配置，然后改写api，才能传递到后端！
15，保存接口开发完成

2/12

1,优化，添加消息提示-----》遇到经典为题:返回promise的异步函数没有用await等待，所以elmessage没生效，太棒了，原来是ELmessage没有正确引入，真该死啊，我的效率太低了。
2，完成开发后端查询接口
3，前段程序开发，完成数据的展示
4，啊我真受不了了，我看我自己写的尸山代码，一个注释都没有，我哪知道我那时候怎么想的
5，完成数据展示前后端功能。
6，开发删除模块后端。完成后端删除功能
7，我的项目太耦合了，我需要把逻辑迁移到父组件，把展示迁移到子组件，这样有利于组件的独立性。看来得把我的vue项目搞一场大迁移了。
8，爷爷组件和孙子组件通信方法:将list列表用pinia统一管理，真烦人啊。
9，pinia统一管理，state和actions，useStore定义方法，实例对象调用方法，解构赋值确定变量。
10，看来是要在pinia的actions里面async函数，正好调用getCat，不要再state调用async函数
11，好感度的架构:好感度操作是一致的，那可以抽出一个通用的好感度方法，然后特异的再各自写，唉。

2/13
1，解决了点击保存就会弹两个信息的bug，需要await，报错不会往下执行
2，解决了删除后不跳转的bug，加个await就行了，原来是没有await删除函数，就往下fetch()，导致冲突就没有生效啊
为了避免奇奇怪怪的bug，最好都加上await，异步异步异步
3，开发互动环节:
架构:是否需要放入pinia？依据是:如果这个方法、数据需要被多个组件供用，就放入pinia，你现在只有一个组件，以后扩展的时候就需要重复调用这个方法，
就必须加入pinia了，比如以后在图片上做文章；
什么时候前端查，什么时候后端查？需要最新数据从后端，查看详情看前端就行，删除也是前端删了后端也删了不存在什么问题
4，开发修改接口，请求体+路径参数传参，请求体前端传对象，后端requestbody接收json转换对象
5，bug:天啊，注意请求路径是/cat/update，而不是/cat/update/，后者直接报404！真该死

2/23
1,我发现成功提示框和错误提示框是一起出现了，然后捣鼓，发现是加个await，这样报错程序可以自动阻塞，不会弹出下面的成功提示框
2，把下面的提示框信息也放到pinia上面去。目标就是玩一玩没有什么错误
3，我的想法是显示chat消息提示，写在前端还是后端？为了前后端分离开发，写在后端。因为后面扩展的时候
负责逻辑的后端更能好好的处理，如加个名字；写在前端就要到前端改逻辑了？合理吗？海军造坦克，陆军造航母，合适吗？
前端负责展示ui交互，后端才负责逻辑
4，页面优化:点击仓库中的猫才展示信息，主要是点击后把currentcat置空，用Object.keys(obj).length===0判断obj是否为{}
5，为什么我点击其他方法没反应？原来参数没写对，遇到错误可以反复比较两者网络请求，比较哪里不对。

3/9
准备加入登录功能
1，后端:完成user三层架构，建了表，建立了JWTutil类，生成token用，controller调用--》验证账号密码，分配token。
2，jwt的secret key用于签名tokne和验证token正确性用的，可以网上生成
3，试图加入路由功能，完成登录

3/10
1,登录页面:前后端都要发力，前端路由守卫，后端真正验证
1.5，完成前端登录模块
2，用了一个小时终于解决了12月15日的难题，原来是组件内部的高度没有设置好，设置好之后，再找到对应的div加个margin就行了
我有点脑雾了，每次遇到这种情况没有认真分析布局，就瞎改，这样效率很低的。ok然后是完成真正的后端的登录认证了。
3，response.getWriter().write("未授权，token 无效");向客户端返回文本的信息
4，完成了拦截器的书写
5，完成了拦截器的注册，前端requets拦截器加入bearer的配合
6，测试类拦截器
7，前后端联调，更改了旧的jwt解析方法。成功通过测试。
8，完成后端登录模块

3/11
1,分用户功能:01,分用户获取猫仓库
1，获取当前用户------------------>token生成withClaims带上userid参数，拦截器解析的时候解析出userid---->int转long没事
   1，从线程中获取userId----------->UserContext工具类
2，查看相应仓库。
3，用户对应猫增删改查功能全部完成，接下来是优化细节
4，修改名称
前端修改名称，发请求。
后端修改
修改-->重新查仓库-->改变currentCat
4,1小细节，可以在改变curr的时候改变ref对象，来改变当前的值
5，删除时确认

3/15
学习了redis短信登录的功能，用这个项目来练习吧；
1，编辑前端界面:可以手机号登录了。加了发送验证码电钮。
2，后端sendcode方法:生成随机数字，保存到redis中。
3，前端验证！直接html加验证就行（新手，基础，简单）;
4，联调sendcode测试。
5，终于明白了
export const sendCode = async (phone) => {
  const sendcode = await request.post("/api/user/sendCode", phone);
  return sendcode;
};是传入的请求头，你所谓的查询参数/api/user/sendCode?phone=13800000000是这样的，其实需要是请求体。
要么前端改成这样:
const sendcode = await request.post("/api/user/sendCode", null, {
  params: { phone: phone }
});
要么后端加上@REQUESTbody注解。
6，isblank比isempty比较字符串多了个比较空白字符的功能。
6，！=比较的是内存地址，euqals比较的是内容
7，引入mybatics-plus依赖
8，配置mybatics:驼峰，日记，全局数据库主键自增。
9，编写实体类，加上注解对应数据库表
10，mapper继承BaseMapper接口
service继承IService<User>
Service继承serviceImpl<BaseMapper,User>
11,在impl中调用this.方法就可以使用userservice带的方法了
12，把passwordlogin成功改造。
13，catcontroller和usercontroller改成mp风格
14，拦截器是在 Spring 完全启动前就被实例化的。所以要构造注入。因为拦截器创建的很早
15，如果用户访问一些不登录就能访问的东西，那就不刷新了吗？那就需要一个拦截一切的拦截器，有就刷新，没有就算了
16，知道了，你在mysql字段名不能用驼峰，必须要用下划线，不然mp出问题的
17，什么玩意？突然一直转打不开，又突然好了，真奇怪！
18，完成了短信登录功能。


3/18
1，加入CacheClient类和redisDAta对象，本土化
2，测试querywith穿透。缓存用在猫s身上吧。

3/20redis实现秒杀的并发问题，
1,基本requsetSender，定义axios对象request，然后两个拦截器请求，还有路由
2，又白忙活一场，调样式，我说怎么莫名缩放了，都是有原因的。？？？？？？？？浪费时间！！！！！
3，现在要做的是获取稀有猫接口
4，数据库设置:
所有稀有猫table--->catid,name,url,store,begintime,endtime
5,准备完成获取稀有猫接口--->完成了获取稀有猫接口。
遇到了304-->没加api，登录问题，还有构造条件的问题。但我对构造条件已经会用了。
3/21
1，完成前端动态展示倒计时。
2，后端RedisWorker全局id生成器。nextID方法实现。increment原子操作⚛️，根据key递增，递增一次返回一次。这样数据就都不想同了
<<二进制左移腾空间，|相当于赋值，把二进制位填上。这样就唯一了。
3，全局id弄好之后，我研究了下单操作，有个扣库存操作卡了我很久。必须要service.update().setsql("11341")才可以,不要用什么queryWrapper，因为这是“query”wrapper。
4，已经加上了乐观锁，就是stock>=0，很简单的。
6，将乐观锁加入了项目，主要还是练了全栈，完成了一个新功能。

3/23

1,新方法:秒杀优化。

- 点击lua脚本，执行这个脚本，存入redis。

  1. 获取两个key，存储stock和下单好的用户的。(传入用户id(判断有没有这个用户)和优惠券id(库存))
  2. 看看库存还够不够。
  3. 看看用户有没有下单过了。
  4. 没有就0

- 根据redis返回数值---->0,1,2.代表成功，库存不足，一人多单

- 成功就将订单(用户，猫，订单id，也许我该加个表？)加入阻塞队列，没啦，业务结束了，

- 同时一个新线程源源不断的干活，干保存订单的活儿。

- 也要获取锁，兜底，如果失败了，但一般不会失败，获取锁成功之后，开新线程保存订单就行了。


发现莫名奇妙“”字符串“”，前端参数加上{}，然后后端用对象接收！你想传递一个string必须前端指定textPlian或者后端手动解析！受得了吗？以后不要传字符串了。


该死的！以后遇到创建bean问题检查依赖有没有导入。

3/26

>学习了发博科点赞关注共同关注feed流之后的实践。

关键就是sortedSet做排行榜、共同关注之set集合SINTER实现交集、feed流推模式：主播放一个东西，推送到粉丝的redis中，粉丝查看可以查看redis中的数据，通过前端实现无限滚动刷新。

##### 1，set集合之SINTER实现，两个用户共同的猫是什么。

1.1,首先实现用户搜索功能。搭建了vue页面。

~~~txt
假使你是一名前端工程师，我需要一个用户搜索的组件。要求如下:
背景铺满浏览器页面，中间主体内容大小为电脑浏览器空白页中间占60-80%，有一个搜索框，用于键入用户名，点击旁边的搜索按钮搜索。下面渲染出分页的数据(后端用pagehelper完成了分页，传出来的数据是一个json数据，封装在QueryUserByName方法，你需要调用接口调用这个方法就行),只能通过用户名模糊搜索，所以前端键入用户名就行，然后用户在下面的列表中渲染出来，单个用户需要有头像(imageUrl)和名称(username)渲染。需要使用elementPLus插件。
~~~

1.2，我要实现分页查询，发现PageHelper和MP是冲突的。悲。我决定用mp，因为项目已经是mp项目了，不好改。

1.3，mp配置分页功能。该死，逆天ai没让我加依赖，3.5.9以后paginationInnerInterceptor已经被独立了，需要独立的jsqlparser依赖，真该死，浪费时间。

>creaete Bean多检查检查依赖，今天又有一个。
>
>什么？get不能用requestbody?去掉了瞬间就好了。怎么没人告诉我？这后面是历史原因，HTTP规范规定，get的body没有定义语义，什么玩意，真该死。
>
>第二个get传递对象的坑，根本不能传递对象！必须要在后边？1&2&3&拼贴才能映射到后端对象当中！什么玩意这是。

1.4分页功能完成！还不错，但是有个小bug，把敏感信息都暴露出来了，要改一下，只展示头像和username

1.5用户功能。

~~~txt
帮我完成一个用户详细界面组件。Vue3+elementPLus
背景铺满浏览器页面，中间主体内容大小为电脑浏览器空白页中间占60-80%
header渲染用户头像和用户名
中间 用ElementPLus的Tabs 标签页组件（目前就填一个标签页就是共同猫猫）

下面展示共同猫猫，在下面的列表中一只猫一只猫渲染出来，单个猫需要有头像(imageUrl)和名称(name)渲染和年龄（age）和catId。需要使用elementPLus插件。

api:你需要调用userView()方法。(就调用就行了，不用catch错误什么的，因为拦截器弄好了)，返回的数据就是这个用户的age,imageUrl,name,catId。

你需要调用queryCatsByUserId()方法，(就调用就行了，不用catch错误什么的，因为拦截器弄好了)，返回的数据就是单只猫的age,imageUrl,name,catId。

多思考几遍，因为我技术不算好，尽量让我看得懂。样式可以好看一点
~~~

##### 1.6开发用户展示页面，什么时候才能用到真技术呢。。

解决思路:发现样式不合我意，去ep官网找对应的，看看他们怎么实现，找到了fixed="right".

1.6.1解决组件router.push之间的参数传递。push

~~~json
router.push（{	
    path:"/view",
    query:{id:row.id}
}）
~~~

，相当于拼贴了，目标组件route.query.id就能获取到。
1.6.2区别:query()链式只支持字符串不支持lamda，lamdaQuery()支持lamda，而wrapper是传统调用。

1.6.3useRouter是行为,useRoute是状态，两者结合可以实现组件路由通信。

##### 1.7后端改造redis，算出共同猫。因为数据库面对这种求即是又是的操作特别费力，这就是为什么要用redis。

~~~js
//分析
现在，我在存入cat的时候，需要加入对应用户的redis当中，key是user:cat:用set
取交集的时候，Inter两个key可以得出交集。
~~~

1.7.1改造以前的代码，使数据存入redis

>学会了前段持久化使用，我不做了，我受不了了，为什么一个currentUser怎么不好做？但躲不掉的，必须要做

~~~js
帮我完成一个无限滚动组件。Vue3+elementPLus
参考这个组件的大小什么的。

我想做feed流，我管理端添加新猫，用户端能接收到，基于分页来搞。

上划是获得新的数据。下划是获得老的数据。

header渲染用户头像和用户名
中间 用ElementPLus的Tabs 标签页组件（目前就填一个标签页就是共同猫猫）

下面的列表中一只猫一只猫渲染出来，单个猫需要有头像(imageUrl)和名称(name)渲染和年龄（age）和catId。需要使用elementPLus插件。

api:你需要调用userView()方法。(就调用就行了，不用catch错误什么的，因为拦截器弄好了)，返回的数据就是这个用户的age,imageUrl,name,catId。

你需要调用queryCatsByUserId()方法，(就调用就行了，不用catch错误什么的，因为拦截器弄好了)，返回的数据就是单只猫的age,imageUrl,name,catId。

多思考几遍，因为我技术不算好，尽量让我看得懂。样式可以好看一点
~~~

